<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Failed</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa; /* Light background */
            font-family: Arial, sans-serif;
        }
        .container {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        .card {
            padding: 30px;
            max-width: 600px;
            width: 100%;
            border: none;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .card-body {
            text-align: center;
        }
        .failed-icon {
            font-size: 60px;
            color: #dc3545; /* Bootstrap danger color */
            margin-bottom: 20px;
        }
        .btn-primary {
            margin-top: 20px;
            padding: 10px 20px;
        }
    </style>
</head>
<body>

<div class="container">
    <div class="card">
        <div class="card-body">
            <div class="failed-icon">
                <i class="bi bi-x-circle"></i> <!-- Bootstrap icon for failure -->
            </div>
            <h1 class="card-title text-danger">Payment Failed</h1>
            <p class="card-text">Unfortunately, your payment was not successful. Please try again or use a different payment method.</p>
            <a href="/user/checkout" class="btn btn-primary">Retry Payment</a>
            <a href="/user/myOrders" class="btn btn-secondary">Go to Orders</a>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
